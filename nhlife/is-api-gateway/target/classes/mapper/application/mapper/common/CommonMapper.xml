<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
		
<mapper namespace="com.soluvis.ds.apigw.v1.application.common.mapper.CommonMapper">
	<insert id="insertBatchLog" parameterType="Map">
		INSERT INTO swm.t_historybatch(service, syskind, jobname, starttime, uuid, server)
		VALUES(#{service}, #{syskind}, #{batchName}, SYSTIMESTAMP, #{uuid}, #{server})
	</insert>
	
	<update id="updateBatchLog" parameterType="Map">
		UPDATE swm.t_historybatch
		SET ENDTIME=SYSTIMESTAMP, STATUS=#{status}, result=#{message}
		WHERE uuid = #{uuid}
	</update>
	
	<resultMap id="CamelHashMap" type="com.soluvis.ds.apigw.v1.util.CamelHashMap">
	</resultMap>
</mapper>